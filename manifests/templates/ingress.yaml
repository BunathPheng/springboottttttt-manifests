---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springboottttttt                  # Ingress name
  labels:
    app: springboottttttt
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springboottttttt.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springboottttttt               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
